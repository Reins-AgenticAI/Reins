# M2-M4 verification

## Implemented

- M2 canonical policy normalization with integer minor units, stable digests, required controls, and fail-closed provider coverage modes.
- M2 two synthetic capability profiles with provenance fields and a comparison page.
- M3 deterministic adversarial corpus covering allowed, threshold, currency mismatch, and merchant mismatch outcomes.
- M4 synthetic authorization, settlement, and order lineage reconciliation with explicit findings.
- M4 digest verification and Ed25519 development-key signature verification for evidence manifests.
- M4 accessible semantic lifecycle timeline and progressive Three.js relationship graph.
- PostgreSQL migration for organization, policy version, coverage assessment, scenario run, and evidence manifest tables.
- Authenticated `/api/assessments` creation/listing and organization-scoped `/api/evidence/:id` export.

## Verification run

- `pnpm format`: passed after formatting; CSS specificity warnings remain.
- `pnpm lint`: passed with six CSS specificity warnings.
- `pnpm typecheck`: passed for API, assurance, database, and web workspaces.
- `pnpm test:unit`: passed, 33 tests across API, assurance, database, and web.
- Web integration: 3 tests passed, including authenticated persistence, evidence export, and missing-evidence isolation.
- `pnpm --filter @reins/web build`: passed; `/assurance` and `/api/assurance` compiled.
- `pnpm db:check`: passed.
- `pnpm db:migrate`: passed against local `reins` PostgreSQL.
- `pnpm db:migrate:test`: passed against local `reins_test` PostgreSQL.

## Limitations

Provider profiles are simulations, not vendor certification. Three.js is presentation-only. The authenticated route is intentionally local and synthetic; it is not a production identity or payment integration. Second-contributor reproduction remains pending. A database reset/backup exercise still needs to be run by both contributors.
