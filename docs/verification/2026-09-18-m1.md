# REINS-001 verification record

Date: 18 September 2026. Branch: `codex/reins-001-foundation`.

Outcome: M1 code is implemented and developer verification passed. M1 is not fully complete. Real PostgreSQL verification is deferred by the user's explicit choice, and second-contributor reproduction is unverified. This is a local synthetic foundation, not a working payment-control application or production certification.

## Five acceptance checks

| Check | Result | Evidence and limits |
| --- | --- | --- |
| Developer quality | Passed | `pnpm format` passed; `pnpm check` passed lint, metadata checks, types, selected regression suites, API build and Next.js production build. |
| Database safety | Passed | Two table-driven guard tests accept three loopback fixtures and reject eleven unsafe or missing inputs. Explicit database integration and test migration fail before connection/migration without valid configuration. Errors do not echo the supplied URL. |
| Real database | Pending | No actual PostgreSQL migrations, connectivity checks or authentication cascade tests executed against a database. User chose to leave this verification pending. |
| Interface behavior | Passed | API HTTP integration suite passed two tests. Three page tests verify truthful scope, navigation targets and planned workflow. The local browser loaded the real page; links and native disclosure worked. |
| Reproduction and accessibility | Partial | README and environment templates document setup. Desktop, 390px and 320px browser layouts were inspected; keyboard skip navigation and disclosure were exercised. Second-contributor setup and a comprehensive accessibility audit remain unverified. |

## Test and command evidence

- Database TDD: the new tests first failed because the target-validation module did not exist. After implementation, the database unit suite passed nine tests.
- UI TDD: all three replacement page checks failed against the previous page. After implementation and corrections, the web unit suite passed twelve tests.
- Five focused top-level tests were added or revised for this work: two database-target tests and three page tests. Input variants and existing regression tests are retained; five tests do not establish exhaustive feature coverage.
- Final developer suites: API 10 passed, database unit 9 passed, web 14 passed. Total 33 passed, zero skipped in these selected suites. Real-database tests are deliberately separate and were not counted as passed.
- The package coverage thresholds passed for their configured source scopes. These limited foundation scopes do not measure the future application, SQL behavior, CSS, or complete security coverage.
- `pnpm db:generate` passed outside the restricted process boundary: four existing tables, no schema changes, no new migration. The earlier restricted invocation emitted `spawn EPERM` and was not treated as successful merely because of its exit status.
- Final `pnpm check:release` repeated the full developer gate successfully on the final code, then exited nonzero at the test-database configuration guard. Consequently the overall release gate did not pass, and its downstream database integration tests did not run.
- The original restricted web build failed with `spawn EPERM`; the same build and complete checks passed with approved execution outside that boundary. No type checking or tests were disabled to obtain a pass.

## UI and review evidence

The navy home page is server rendered and labels synthetic-only scope, current foundation work and future capabilities. Active links point to implemented sections. Setup instructions distinguish development commands from the database-required release gate. The planned Three.js view remains in M4, with timeline/table alternatives.

Browser inspection confirmed readable desktop and narrow layouts. At a 320px viewport, the final document client and scroll widths both measured 305px, with the scrollbar accounting for the difference. The release instructions used one grid column and readable wrapped commands. Keyboard Tab exposed the skip link; Enter moved to main content. Section navigation and keyboard expansion of the native disclosure worked. This is focused behavioral inspection, not a WCAG conformance certification.

A separate static reviewer checked the scoped files against the plan. One actionable mobile-grid finding was fixed and then inspected in the browser. No remaining actionable findings were reported in that scope. The repository has no initial commit, so review used current file content rather than claiming a meaningful committed diff. This automated review does not replace the other product owner's review before merging critical work.

## Implementation scope

- Test infrastructure: `packages/db/test/database-target.ts`, its tests, `verify-target.ts`, and the existing real-database integration suite.
- Commands and configuration: root and database `package.json`, `compose.yaml`, `.env.test.example`, `.gitignore`, and `.github/workflows/ci.yml`.
- UI: `apps/web/src/app/page.tsx`, `page.test.tsx`, and `globals.css`.
- Documentation: README, M1 architecture note, complete milestone roadmap, executable M1 plan, task tracker, lessons and this record.

No new production dependency, paid resource, external financial account, production credential, commit, push, or merge was introduced. GitHub CI configuration was updated but no remote CI run is claimed. PostgreSQL installation was not completed and no further installation is planned while verification is deferred. The Word PRD was not changed by this implementation task.

## Remaining work

M1 still requires real isolated PostgreSQL migration/integration evidence and second-contributor setup reproduction. M2 through M5 remain planned. Their policy, authorization, approval, reconciliation, protocol and Three.js behaviors have not been implemented or tested by these foundation checks.
