POSTGRES_USER=reins
POSTGRES_PASSWORD=reins_local_only
POSTGRES_DB=reins
POSTGRES_PORT=5432
DATABASE_URL=postgresql://reins:reins_local_only@127.0.0.1:5432/reins
BETTER_AUTH_URL=http://localhost:3000
BETTER_AUTH_SECRET=local-development-only-change-me-0123456789
API_HOST=127.0.0.1
API_PORT=4000
LOG_LEVEL=info
LLM_PROVIDER=simulation
OLLAMA_ENDPOINT=http://127.0.0.1:11434/api/generate
OLLAMA_MODEL=qwen3:4b
# GROQ_API_KEY is set only in Vercel Project Settings. Never commit it.
STRIPE_MODE=test
# Set to true only in a protected local or authenticated test environment.
STRIPE_TEST_EXECUTION_ENABLED=false
# STRIPE_SECRET_KEY=rk_test_... (local/Vercel sensitive server variable only)
# STRIPE_WEBHOOK_SECRET=whsec_... (set after Stripe CLI or Dashboard webhook configuration)
